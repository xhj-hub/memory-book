# 记忆账本 PWA · GitHub Pages 部署步骤

这个文件夹是一个**可离线安装的网页 App**。部署到 GitHub Pages 后：
- iPad「添加到主屏幕」就有独立 App 图标，**断网也能打开使用**；
- 学生的错题与复习进度存在 iPad 浏览器的 localStorage 里，**永不上传**（GitHub 上只有页面代码，不含任何学习数据）。

文件清单：`index.html`、`manifest.webmanifest`、`sw.js`、`icon-192.png`、`icon-512.png`、`apple-touch-icon.png`、`.nojekyll`

---

## 方式 A：网页拖拽上传（最简单，无需命令行）

1. 登录 github.com → 右上角 **+** →「New repository」。
2. 仓库名填 `memory-book`（随意），选 **Public**（Pages 免费版需要公开仓库；页面无数据，公开无隐私问题），点 Create。
3. 进入空仓库页 →「uploading an existing file」→ 把本文件夹里**全部文件**（含 `.nojekyll`）拖进去 → Commit。
4. 仓库顶部 **Settings** → 左侧 **Pages** → Source 选「Deploy from a branch」→ Branch 选 `main` / 目录 `/ (root)` → Save。
5. 等 1~2 分钟，页面顶部会出现网址：
   `https://<你的用户名>.github.io/memory-book/`

## 方式 B：命令行 git

在本文件夹执行（把 <你的用户名> 换成实际用户名，需先在 github 建好空仓库 memory-book）：

```
cd /Users/eleme/Documents/qoder/study_memory_buddy/pwa
git init
git add -A
git commit -m "记忆账本 PWA"
git branch -M main
git remote add origin https://github.com/<你的用户名>/memory-book.git
git push -u origin main
```

推送后同样到 **Settings → Pages** 开启（Branch: main / root）。

---

## iPad 上安装

1. iPad Safari 打开 `https://<你的用户名>.github.io/memory-book/`
2. 点底部**分享**按钮 →「**添加到主屏幕**」→ 完成。
3. 主屏幕出现「记忆账本」图标，点开像原生 App，**之后断网也能用**。
4. 首次联网打开一次即可完成离线缓存。

## 更新页面后怎么办

改了代码重新部署后，把 `sw.js` 里的 `memory-book-v1` 改成 `v2`（每次 +1），
iPad 上重开 App 会自动拉新版本并提示"已更新，离线可用"。

---

## 和千问的衔接（回顾）

千问「错题记忆助手」整理错题 → 复制它给出的 JSON → 记忆账本「导入导出」页签粘贴导入 → 每天在「今日复习」按艾宾浩斯复习。
